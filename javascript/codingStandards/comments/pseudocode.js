// function which takes in an array 'scores' as an argument, and returns the average of the elements in the array
function calculateAverageScore(scores) {
    // Initialize a variable "total" to 0

    // For each score in the array "scores"
        // Add the score to "total"

    // Divide "total" by the length of "scores" to get the average

    // Return the average score
}

// function which validates whether the given login information is correct
function logIn(username, password) {
    // Check if both username and password are provided
        // If not, display an error message and return false

    // Search the database for a user that matches the provided username
        // If no user is found, display an error message and return false

    // Check if the provided password matches the user's password in the database
        // If not, display an error message and return false

    // If all checks pass, display a login succeeded message and return true
}

/* Calculator program that helps the user perform a mathematical operation with two numbers */

// Begin Program

// Display a menu with options for add, subtract, multiply, or divide

// Get user input for operation choice

// If user chose add
    // Prompt user for two numbers
    // Add the numbers
    // Display the result

// Else If user chose subtract
    // Prompt user for two numbers
    // Subtract the numbers
    // Display the result

// Else If user chose multiply
    // Prompt user for two numbers
    // Multiply the numbers
    // Display the result

// Else If user chose divide
    // Prompt user for two numbers
    // Divide the first number by the second number
    // Display the result

// End Program
